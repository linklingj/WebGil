{
  "name": "webgil",
  "version": "0.1.0",
  "private": true,
  "description": "구조 기반 스크린리더 — 셸 독립 코어 + CaptureSource",
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
